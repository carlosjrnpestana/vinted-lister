name: Publish Vinted Listing

on:
  workflow_dispatch:
    inputs:
      ITEM_TITLE:
        description: 'Listing Title'
        required: true
        type: string
      ITEM_DESC:
        description: 'Listing Description'
        required: true
        type: string
      ITEM_CATEGORY:
        description: 'Category Path (e.g. Casa › Acessórios para a casa › Decoração de parede)'
        required: true
        type: string
      ITEM_PRICE:
        description: 'Price in EUR'
        required: true
        type: string
      ITEM_BRAND:
        description: 'Brand (Optional)'
        required: false
        type: string
      IMAGE_URLS:
        description: 'Comma-separated Image URLs'
        required: true
        type: string

jobs:
  publish-listing:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install

      - name: Install Playwright browsers and system dependencies
        run: npx playwright install --with-deps chromium

      - name: Run Vinted Listing Automation
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GOOGLE_GENERATIVE_AI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          VINTED_COOKIES: ${{ secrets.VINTED_COOKIES }}
          ITEM_TITLE: ${{ inputs.ITEM_TITLE }}
          ITEM_DESC: ${{ inputs.ITEM_DESC }}
          ITEM_CATEGORY: ${{ inputs.ITEM_CATEGORY }}
          ITEM_PRICE: ${{ inputs.ITEM_PRICE }}
          ITEM_BRAND: ${{ inputs.ITEM_BRAND }}
          IMAGE_URLS: ${{ inputs.IMAGE_URLS }}
        run: xvfb-run --auto-servernum -- npm start
